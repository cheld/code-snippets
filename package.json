{
  "name":"code-snippets",
  "private": true,
  "devDependencies":{
    "http-server": "^0.12.3"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1"
  }
}
